# redis-exporter configuration
redis_addr: "redis://redis-parking-cache:6379"
redis_password: "${REDIS_PASSWORD}"
namespace: "redis"
check-keys: "parking:*"
script: |
  #!/bin/bash
  # Custom Lua script for parking-specific metrics
  local keys = redis.call('KEYS', 'parking:*')
  return #keys